# Core Framework
Django>=5.1,<5.2
wagtail==7.2.1

# Database drivers
psycopg2-binary>=2.9

# Wagtail dependencies (usually installed with wagtail, but explicit for clarity)
django-modelcluster>=6.0
django-taggit>=5.0
Pillow>=10.0

# Production server (optional, for deployment)
gunicorn>=20.0.0

whitenoise>=6.6.0      # For serving static CSS/JS
django-storages>=1.14  # For storing images on DO Spaces
boto3>=1.34           # AWS S3/Spaces SDK